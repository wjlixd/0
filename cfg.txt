#C:\Users\Administrator 目录下面
[user]
	name = wjlixd
	email = 734665111@qq.com
[alias]
	cm = commit -a -m
#克隆仓库   git clone http://github.com/wjlixd/tx.git
#操作TX仓库
	upt = push http://github.com/wjlixd/tx.git tx
	dnt = pull http://github.com/wjlixd/tx.git tx
#操作RX仓库
	upr = push http://github.com/wjlixd/rx.git rx 
	dnr = pull http://github.com/wjlixd/rx.git rx 
#保存暂时
        ss  = stash save
#应用缓冲
        sa  = stash apply
#缓冲列表
	sl  = stash list
#清除所有缓冲    git stash clear
#删除某一个缓冲  git stash drop [id]

#撤消修改
	rst = reset --hard
#删除没有跟踪的文件
	cln = clean -f
	md = commit --amend
	st = status
	gdf = gitdiff
	l = log
	cp = cherry-pick
	b = branch
	rc = rm -r --cached .
	co = checkout
	mg = merge
	dt = difftool
	mt = mergetool    last = log -1 HEAD
	cf = config
	cfe = config --global -e
	line = log --oneline
	latest = for-each-ref --sort=-committerdate --format='%(committerdate:short) %(refname:short) [%(committername)]'	  
	ls = log --pretty=format:\"%C(yellow)%h %C(blue)%ad %C(red)%d %C(reset)%s %C(green)[%cn]\" --decorate --date=short
	hist = log --pretty=format:\"%C(yellow)%h %C(red)%d %C(reset)%s %C(green)[%an] %C(blue)%ad\" --topo-order --graph --date=short
	type = cat-file -t
dump = cat-file -p
